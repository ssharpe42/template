# STATE — hot handoff state

> Rewritten whole at every `/handoff` by the historian. Hard cap ~80 lines: this file
> is injected into every session at boot, so every line taxes every future session.
> History belongs in LEDGER.md, not here.

## Current goal
Loom freshly installed — no work in flight yet.

## In flight
Nothing. Working tree expected clean.

## Next action
Run `/map` to build the codebase atlas, then take the first task through
`/training-change`.

## Live gotchas
- `context/MAP.md` is a placeholder until `/map` runs.
- `context/failures/catalog.md` holds only the seeded generic failure modes — it
  earns its keep as this repo's own incidents get banked into it.

## Branch / repo state
- Branch: (record at first handoff)
- Last memory commit: (record at first handoff)
