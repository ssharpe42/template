# Codebase Atlas

> Last refreshed: never · Owner: cartographer · Refresh via /map

This is a placeholder. Run `/map` to have the cartographer build the atlas:
orientation-in-60-seconds, subsystem breakdown (key files, flow, config surface,
landmines), cross-cutting concerns (seeding, precision, distributed, checkpointing),
and test topology.

Until then, scouts and planners must explore from scratch — which is exactly the
cost the atlas exists to eliminate.
