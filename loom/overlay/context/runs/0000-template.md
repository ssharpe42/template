# RUN-0000: <run name / purpose>

> Template — copy to the next free number (`NNNN-slug.md`). Written by the
> historian, usually via /handoff (runs worth remembering) or /postmortem (runs
> that went wrong).

- **Date(s):**
- **Git sha / branch:**
- **Config:** <path + the overrides that mattered>
- **Seed / data version:**
- **Hardware / world size:**
- **Launch command:**

## Outcome
one line: succeeded / failed / suspicious — and the headline numbers.

## Timeline
start → notable events (anomaly onset, restarts, interventions) → end.

## Findings
What this run taught us. For failures: root cause with evidence (or explicit
UNRESOLVED), the fix, and the catalog ID if a new failure mode was banked.

## Artifacts
checkpoints, metric dashboards, log locations.
